ТЕМА: СТРОКОВЫЕ ПОТОКИ
    istringstream is {"2 3 4"}
    В стеке размещаются локальные перменные функции, а так же служебная 
информация для возврата.
    В куче размещаются объекты которые живут дольше



#include <chrono>
using namespace::chrono;

struct Gadgets ()
{
    string name;
    string number;
    string lord;
}
vector <Gadgets> Agregator() 
{
    vector <Gadgets> g(1'000'000); //миллион гаджетов которые имеют lord number name
    return g;
}
int main () 
{   
    auto start = steady_clock::now();
    vector<Gadgets> gdg = Agregator();
    auto finish = steady_clock::now();
    cout << duration_cast<millisecond> (finish - start).count; //время за сколько работает всё между стартом и финишем
}




    ДОБАВЛЕНИЕ ССЫЛКИ УСКОРЯЕТ РАБОТУ ПОТОМУ ЧТО ПЕРЕМЕННЫЕ НЕ В СТЕК КОПИРУЮТСЯ А НЕПОДСРЕСТВЕННОЕ РАБОТА ПРОИСХОДИТ С АДРЕСАМИ

    ВЫВОД: 1. ДЛЯ ПЕРЕДАЧИ МАЛЕНЬКИХ ЗНАЧЕНИЙ СЛЕДУЕТ ИСПОЛЬЗОВАТЬ ПЕРЕДАЧУ ОБЪЕКТОВ ПО ЗНАЧЕНИЮ. 2. ДЛЯ ПЕРЕДАЧИ БОЛЬШИХ ОБЪЕКТОВ, КОТОРЫЕ НЕЛЬЗЯ МЕНЯТЬ ПРОИСХОДИТ ПЕРЕДАЧА ПО КОНСТАНТНОЙ ССЫЛКЕ. 3. СЛЕДУЕТ ВОЗВРАЩАТЬ ОБЪЕКТ А НЕ МОДИФИЦИРОВАТЬ РЕЗУЛЬТАТ. 4. ПЕРЕДАЧУ ПО ССЫЛКЕ НУЖНО ИСПОЛЬЗОВАТЬ ТОЛЬКО В САМЫХ НЕОБХОДИМЫХ СЛУЧАЯХ.

    operator << ("Аргументы"); - это пример полиморфизма (в данном случае для вывода) c++ (это перегрузка функций или операторов), которая позволяет использвать одинаковые операторы для аргументов разных типов.
    
    операции ввода/вывода основывается на иерархии потоков ввода вывода

    void operator << (ostream& os, vertor<int> v)
    {
    for (int i : v)
        {
            cout <<  i; //Сможет ТОЛЬКО ВЫВЕСТИ ostream
        }
    }
    ПОСМОТРЕТЬ ЧТО ТАКОЕ operator В ИНТЕРНЕТЕ И ВСЕ ОПЕРАЦИИ КОТОРЫЕ МОЖНО ПЕРЕГРУЖАТЬ



ТЕМА: ПЕРЕГРУЗКА АРИФМЕТИЧЕСКИХ ОПЕРАЦИЙ
    
    vector<int> operator + (vector<int> v1, vector<int> v2)
        {
          vector<int> result;
            
          //здесь например складываем вектора поэлементо.

          return result;
        }

    main () 
    {
        v3 = v1 + v2;
        cout << v3 << endl; // Здесь уже смогут сложиться аж два веткторая так как операцию + мы перегрузили и все элементы v1 и v2 соотвественно сложатся поэлеметно 
    }


ТЕМА: ШАБЛОННАЯ ФУНКЦИЯ

    template <typename T> //Т - это шаблон для типа (например можно задать переменную а типа Т: T a = 0;) и это часть протопита функции в самом начале
    template <typename T> vector<T> operator + (vector<T> v1, vector<T> v2) {/////}





















